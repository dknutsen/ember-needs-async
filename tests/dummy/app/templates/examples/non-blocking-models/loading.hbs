{{!--
  because the parent outlet isn't rendered until the crucial bits
  of the parent model are loaded this template will never actually
  be rendered, but it still needs to be defined so the parent route
  doesn't get blocked waiting for the child hooks
--}}
